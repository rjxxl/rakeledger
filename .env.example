DATABASE_URL="postgresql://rakeledger:rakeledger_dev@localhost:5432/rakeledger?schema=public"
# DIRECT_URL is used by Prisma migrations (it bypasses pgbouncer when present in production).
# In local dev there's no pooler — set both to the same connection string.
DIRECT_URL="postgresql://rakeledger:rakeledger_dev@localhost:5432/rakeledger?schema=public"
